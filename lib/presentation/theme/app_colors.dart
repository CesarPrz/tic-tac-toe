import 'dart:ui';

abstract final class AppColors {
  static const canvasBackground = Color(0xFF9C4A12);
  static const accent = Color(0xFFF2B705);
  static const ink = Color(0xFF000000);
  static const surface = Color(0xFF242019);
  static const surfacePressed = Color(0xFF171310);
  static const scrollMark = Color(0x29FFFFFF);
}
